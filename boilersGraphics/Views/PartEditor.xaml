<UserControl
    x:Class="boilersGraphics.Views.PartEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:parts="clr-namespace:boilersGraphics.UserControls.Parts"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="600"
    d:DesignWidth="900"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="Title" Value="{Binding Title}" />
            <Setter Property="Width" Value="900" />
            <Setter Property="Height" Value="600" />
            <Setter Property="MinWidth" Value="480" />
            <Setter Property="MinHeight" Value="320" />
            <Setter Property="ResizeMode" Value="CanResize" />
            <Setter Property="ShowInTaskbar" Value="True" />
        </Style>
    </prism:Dialog.WindowStyle>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/boilersGraphics;component/Resources/Theme.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/boilersGraphics;component/Resources/Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel LastChildFill="True">
        <!--  Toolbar (Phase 1-c-6-a: placeholder)  -->
        <Border
            Padding="8,4"
            Background="#2D2D30"
            BorderBrush="#3F3F46"
            BorderThickness="0,0,0,1"
            DockPanel.Dock="Top">
            <TextBlock
                FontWeight="Bold"
                Foreground="#F0F0F0"
                Text="{Binding Title}" />
        </Border>

        <!--  Bottom command bar  -->
        <Border
            Padding="8"
            Background="#252526"
            BorderBrush="#3F3F46"
            BorderThickness="0,1,0,0"
            DockPanel.Dock="Bottom">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    Width="96"
                    Command="{Binding CloseCommand}"
                    Content="閉じる"
                    IsCancel="True" />
            </StackPanel>
        </Border>

        <!--  Center editing area (Phase 1-c-6-d-1: Rectangle 追加のみ)  -->
        <parts:PartEditorDesignerControl />
    </DockPanel>
</UserControl>
