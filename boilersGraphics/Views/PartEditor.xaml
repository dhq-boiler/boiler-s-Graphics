<UserControl
    x:Class="boilersGraphics.Views.PartEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="600"
    d:DesignWidth="900"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="Title" Value="{Binding Title}" />
            <Setter Property="Width" Value="900" />
            <Setter Property="Height" Value="600" />
            <Setter Property="MinWidth" Value="480" />
            <Setter Property="MinHeight" Value="320" />
            <Setter Property="ResizeMode" Value="CanResize" />
            <Setter Property="WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="ShowInTaskbar" Value="True" />
        </Style>
    </prism:Dialog.WindowStyle>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/boilersGraphics;component/Resources/Theme.xaml" />
                <ResourceDictionary
                    Source="pack://application:,,,/boilersGraphics;component/Resources/Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel LastChildFill="True">
        <!--  Toolbar (Phase 1-c-6-a: placeholder)  -->
        <Border
            Padding="8,4"
            Background="#2D2D30"
            BorderBrush="#3F3F46"
            BorderThickness="0,0,0,1"
            DockPanel.Dock="Top">
            <TextBlock
                FontWeight="Bold"
                Foreground="#F0F0F0"
                Text="{Binding Title}" />
        </Border>

        <!--  Bottom command bar  -->
        <Border
            Padding="8"
            Background="#252526"
            BorderBrush="#3F3F46"
            BorderThickness="0,1,0,0"
            DockPanel.Dock="Bottom">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button
                    Width="96"
                    Command="{Binding CloseCommand}"
                    Content="閉じる"
                    IsCancel="True" />
            </StackPanel>
        </Border>

        <!--  Center canvas: read-only preview of PartDefinition.Items (Phase 1-c-6-c)  -->
        <Grid Background="#1E1E1E">
            <ScrollViewer
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Definition.Items}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas
                                MinWidth="400"
                                MinHeight="300"
                                Background="Transparent" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding Left.Value}" />
                            <Setter Property="Canvas.Top" Value="{Binding Top.Value}" />
                            <Setter Property="Panel.ZIndex" Value="{Binding ZIndex.Value}" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </ScrollViewer>
            <TextBlock
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="8,8,0,0"
                FontSize="10"
                Foreground="#80808080"
                Text="(読み取り専用プレビュー — 編集機能は Phase 1-c-6-d 以降)" />
        </Grid>
    </DockPanel>
</UserControl>
