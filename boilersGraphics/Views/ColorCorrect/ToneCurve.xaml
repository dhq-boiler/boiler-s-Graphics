<UserControl x:Class="boilersGraphics.Views.ColorCorrect.ToneCurve"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converter="clr-namespace:boilersGraphics.Converters"
             xmlns:helpers="clr-namespace:boilersGraphics.Helpers"
             xmlns:viewModels="clr-namespace:boilersGraphics.ViewModels"
             xmlns:colorCorrect="clr-namespace:boilersGraphics.ViewModels.ColorCorrect"
             xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converter:ToToneCurveControlPointCollectionConverter x:Key="ToPointCollection" />
        <converter:ToneCurvePointConverter x:Key="ToneCurvePointConverter" />
    </UserControl.Resources>
    <DockPanel>
        <Border BorderBrush="white" Width="300" Height="300" BorderThickness="1">
            <Canvas Width="300" Height="300">
                <Path Width="300"
                      Height="300"
                      Stroke="Black"
                      StrokeThickness="1"
                      Stretch="Fill">
                    <Path.Data>
                        <PathGeometry>
                            <PathFigure StartPoint="{Binding Points[0], Converter={StaticResource ToneCurvePointConverter}}">
                                <PolyBezierSegment Points="{Binding Points, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource ToPointCollection}}"></PolyBezierSegment>
                            </PathFigure>
                        </PathGeometry>
                    </Path.Data>
                </Path>
                <ItemsControl ItemsSource="{Binding Points}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas x:Name="ItemsControlCanvas" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Thumb>
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="DragDelta">
                                        <helpers:EventToCommand Command="{Binding Path=DataContext.DragDeltaCommand, ElementName=ItemsControlCanvas}"
                                                                PassEventArgsToCommand="True"></helpers:EventToCommand>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </Thumb>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="Canvas.Left" Value="{c:Binding X.Value-2}"></Setter>
                            <Setter Property="Canvas.Top" Value="{c:Binding Y.Value-2}"></Setter>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </Canvas>
        </Border>
    </DockPanel>
</UserControl>