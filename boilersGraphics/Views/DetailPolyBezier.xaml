<UserControl
    x:Class="boilersGraphics.Views.DetailPolyBezier"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helper="clr-namespace:boilersGraphics.Helpers"
    xmlns:local="clr-namespace:boilersGraphics.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:views="clr-namespace:boilersGraphics.Views"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{StaticResource BackgroundColour}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/boilersGraphics;component/Resources/Theme.xaml" />
                <ResourceDictionary Source="pack://application:,,,/boilersGraphics;component/Resources/Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="Width" Value="640" />
            <Setter Property="Height" Value="480" />
        </Style>
    </prism:Dialog.WindowStyle>
    <DockPanel LastChildFill="True">
        <DockPanel DockPanel.Dock="Bottom">
            <DockPanel DockPanel.Dock="Right">
                <Button
                    Width="100"
                    Margin="5"
                    HorizontalAlignment="Right"
                    Command="{Binding OKCommand}">
                    OK
                </Button>
            </DockPanel>
        </DockPanel>
        <DockPanel Width="Auto" DockPanel.Dock="Right">
            <ListView ItemsSource="{Binding Properties}">
                <ListView.View>
                    <GridView>
                        <GridView.Columns>
                            <GridViewColumn
                                Width="Auto"
                                DisplayMemberBinding="{Binding Path=PropertyName.Value}"
                                Header="{Binding Source={x:Static helper:ResourceService.Current}, Path=Resources.String_PropertyName}" />
                            <GridViewColumn Width="Auto" Header="{Binding Source={x:Static helper:ResourceService.Current}, Path=Resources.String_PropertyValue}">
                                <GridViewColumn.CellTemplateSelector>
                                    <views:OptionsContentTemplateSelector>
                                        <views:OptionsContentTemplateSelector.ComboBoxTemplate>
                                            <DataTemplate>
                                                <ComboBox
                                                    x:Name="ComboBox_PropertyValue"
                                                    HorizontalAlignment="Stretch"
                                                    ItemsSource="{Binding Options}"
                                                    SelectedValue="{Binding PropertyValue, Mode=TwoWay}">
                                                    <ComboBox.ItemTemplateSelector>
                                                        <views:ComboBoxItemTemplateSelector>
                                                            <DataTemplate DataType="{x:Type PenLineJoin}">
                                                                <Grid Background="White">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="50" />
                                                                        <ColumnDefinition Width="50" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="60" />
                                                                    </Grid.RowDefinitions>
                                                                    <Polyline
                                                                        Grid.Row="0"
                                                                        Grid.Column="0"
                                                                        HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"
                                                                        Points="10,20 20,5 30,40 40,20"
                                                                        Stroke="{Binding DataContext.Object.Value.EdgeBrush.Value, ElementName=ComboBox_PropertyValue}"
                                                                        StrokeLineJoin="{Binding}"
                                                                        StrokeThickness="10" />
                                                                    <Label
                                                                        Grid.Row="0"
                                                                        Grid.Column="1"
                                                                        VerticalAlignment="Center"
                                                                        Foreground="Black">
                                                                        <Label.Content>
                                                                            <TextBlock Foreground="Black">
                                                                                <Run Text="{Binding Mode=OneWay}" />
                                                                            </TextBlock>
                                                                        </Label.Content>
                                                                    </Label>
                                                                </Grid>
                                                            </DataTemplate>
                                                            <DataTemplate DataType="{x:Type PenLineCap}">
                                                                <Grid Background="White">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="65" />
                                                                        <ColumnDefinition Width="60" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="30" />
                                                                    </Grid.RowDefinitions>
                                                                    <Line
                                                                        Grid.Row="0"
                                                                        Grid.Column="0"
                                                                        Stroke="{Binding DataContext.Object.Value.EdgeBrush.Value, ElementName=ComboBox_PropertyValue}"
                                                                        StrokeEndLineCap="{Binding}"
                                                                        StrokeThickness="20"
                                                                        X1="0"
                                                                        X2="55"
                                                                        Y1="15"
                                                                        Y2="15" />
                                                                    <Label
                                                                        Grid.Row="0"
                                                                        Grid.Column="1"
                                                                        VerticalAlignment="Center"
                                                                        Foreground="Black">
                                                                        <Label.Content>
                                                                            <TextBlock Foreground="Black">
                                                                                <Run Text="{Binding Mode=OneWay}" />
                                                                            </TextBlock>
                                                                        </Label.Content>
                                                                    </Label>
                                                                </Grid>
                                                            </DataTemplate>
                                                        </views:ComboBoxItemTemplateSelector>
                                                    </ComboBox.ItemTemplateSelector>
                                                </ComboBox>
                                            </DataTemplate>
                                        </views:OptionsContentTemplateSelector.ComboBoxTemplate>
                                        <views:OptionsContentTemplateSelector.TextBoxTemplate>
                                            <DataTemplate>
                                                <TextBox
                                                    HorizontalAlignment="Stretch"
                                                    HorizontalContentAlignment="{Binding HorizontalContentAlignment.Value}"
                                                    Text="{Binding PropertyValue, Mode=TwoWay}" />
                                            </DataTemplate>
                                        </views:OptionsContentTemplateSelector.TextBoxTemplate>
                                    </views:OptionsContentTemplateSelector>
                                </GridViewColumn.CellTemplateSelector>
                            </GridViewColumn>
                        </GridView.Columns>
                    </GridView>
                </ListView.View>
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}">
                        <Setter Property="SnapsToDevicePixels" Value="True" />
                        <Setter Property="Padding" Value="4,1" />
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderBrush" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListViewItem}">
                                    <Border
                                        x:Name="Bd"
                                        Padding="2"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        SnapsToDevicePixels="true">
                                        <GridViewRowPresenter HorizontalAlignment="{TemplateBinding HorizontalAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="True" />
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlMouseOverBackground}" />
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource ControlMouseOverBorderBrush}" />
                                        </MultiTrigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="Selector.IsSelectionActive" Value="False" />
                                                <Condition Property="IsSelected" Value="True" />
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlDefaultBackground}" />
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource ControlDefaultBorderBrush}" />
                                        </MultiTrigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="Selector.IsSelectionActive" Value="True" />
                                                <Condition Property="IsSelected" Value="True" />
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlSelectedBackground}" />
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource ControlSelectedBorderBrush}" />
                                        </MultiTrigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="Bd" Property="TextElement.Foreground" Value="{StaticResource ControlDisabledGlythColour}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </DockPanel>
        <local:DetailLine Content="{Binding}" DataContext="{Binding ViewModel.Value}" />
    </DockPanel>
</UserControl>
