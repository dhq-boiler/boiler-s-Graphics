<UserControl x:Class="boilersGraphics.Views.DetailPoint"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:control="clr-namespace:boilersGraphics.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:boilersGraphics.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type control:DoubleTextBox}">
                <Setter Property="Width" Value="50" />
            </Style>
        </Grid.Resources>
        <StackPanel Orientation="Horizontal">
            <Label Content="X" />
            <control:DoubleTextBox HorizontalContentAlignment="Right"
                                   VerticalContentAlignment="Center"
                                   DoubleText="{Binding X, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Y" />
            <control:DoubleTextBox HorizontalContentAlignment="Right"
                                   VerticalContentAlignment="Center"
                                   DoubleText="{Binding Y, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Grid>
</UserControl>
